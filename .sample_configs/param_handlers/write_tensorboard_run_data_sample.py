def make_tensorboard_run(tensorboard_run: str) -> str:
    tensorboard_run = tensorboard_run

    return tensorboard_run

def make_time_series_data(time_series_data: typing.Sequence[google.cloud.aiplatform_v1beta1.types.tensorboard_data.TimeSeriesData]) -> typing.Sequence[google.cloud.aiplatform_v1beta1.types.tensorboard_data.TimeSeriesData]:
    time_series_data = time_series_data

    return time_series_data

