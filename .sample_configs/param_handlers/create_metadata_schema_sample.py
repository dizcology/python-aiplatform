def make_parent(parent: str) -> str:
    parent = parent

    return parent

def make_metadata_schema(metadata_schema: google.cloud.aiplatform_v1beta1.types.metadata_schema.MetadataSchema) -> google.cloud.aiplatform_v1beta1.types.metadata_schema.MetadataSchema:
    metadata_schema = metadata_schema

    return metadata_schema

def make_metadata_schema_id(metadata_schema_id: str) -> str:
    metadata_schema_id = metadata_schema_id

    return metadata_schema_id

