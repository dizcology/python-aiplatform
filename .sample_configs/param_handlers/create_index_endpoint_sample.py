def make_parent(parent: str) -> str:
    parent = parent

    return parent

def make_index_endpoint(display_name: str) -> google.cloud.aiplatform_v1beta1.types.index_endpoint.IndexEndpoint:
    index_endpoint = {'display_name': display_name}

    return index_endpoint

