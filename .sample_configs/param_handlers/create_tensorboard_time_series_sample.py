def make_parent(parent: str) -> str:
    parent = parent

    return parent

def make_tensorboard_time_series(display_name: str) -> google.cloud.aiplatform_v1beta1.types.tensorboard_time_series.TensorboardTimeSeries:
    tensorboard_time_series = {'display_name': display_name}

    return tensorboard_time_series

