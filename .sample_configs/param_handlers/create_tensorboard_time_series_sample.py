def make_parent(parent: str) -> str:
    parent = parent

    return parent

def make_tensorboard_time_series(tensorboard_time_series: google.cloud.aiplatform_v1beta1.types.tensorboard_time_series.TensorboardTimeSeries) -> google.cloud.aiplatform_v1beta1.types.tensorboard_time_series.TensorboardTimeSeries:
    tensorboard_time_series = tensorboard_time_series

    return tensorboard_time_series

